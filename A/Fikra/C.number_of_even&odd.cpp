//C. number of even & odd

#include <iostream>
using namespace std;

int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);
    long long x;
    cin >> x;
    cout << x /2 << " " << (x/2) + (x % 2);

    

    return 0;
}